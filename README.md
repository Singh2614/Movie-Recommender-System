# Movie-Recommender-System

Designed a content-based movie recommender system using TF-IDF vectorization with feature selection
– Implemented cosine similarity and dynamic user input to generate customizable movie recommendations.
